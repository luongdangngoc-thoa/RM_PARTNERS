<div id="guide-delivery-cost" class="section-{{ section.id }} gradient color-{{ section.settings.section_color_scheme }} {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}">
  <div class="delivery-cost-wrapper page-width-desktop page-width-tablet page-width-sp">
    <h2 class="delivery-cost-title">
      配送料について</h2>
    <div class="delivery-cost-list">
      <p class="delivery-cost-txt">
        配送料は、以下の表の通りでございます。 <br>
        ※配送料はお客様にて着払いでのお支払いになります。<br>
        ※特別配送のため、事前に正式な配送料をご提示することは不可能でございますので、予めご了承くださいませ。<br>
      </p>

      <div class="delivery-cost-table-wrapper">
        <div class="delivery-cost-table">
          <div class="delivery-cost-table-col col-first">
            <span>
              サイズ名 (重量)</span>
            <span>
              80 (5kg)</span>
            <span>
              100 (10kg)</span>
            <span>
              140 (20kg)</span>
            <span>
              160 (30kg)</span>
          </div>

          {%- for block in section.blocks -%}
            <div class="delivery-cost-table-col">
              <span>
                {{ block.settings.delivery-area }}</span>
              <span>
                {{ block.settings.delivery-cost-1 }}</span>
              <span>
                {{ block.settings.delivery-cost-2 }}</span>
              <span>
                {{ block.settings.delivery-cost-3 }}</span>
              <span>
                {{ block.settings.delivery-cost-4 }}</span>
            </div>

          {%- endfor -%}
        </div>
      </div>

      <p class="delivery-cost-txt">
        出荷に関しまして、佐川急便様にて発送いたします。 <br>
        施工現場やクライアント様への直接納品、代理出荷も承っておりますのでお気軽にご相談ください。
      </p>
    </div>
  </div>
</div>

<style>
  #guide-delivery-cost {
    padding-top: 8rem;
  }
  .delivery-cost-title {
    color: var(--primary, #333);
    font-family: Noto Serif JP;
    font-size: 2rem;
    font-style: normal;
    font-weight: 400;
    line-height: calc(30 / 20);
    letter-spacing: 1px;
  }

  .delivery-cost-txt {
    margin: 0;
    color: var(--primary, #333);
    font-family: Noto Serif JP;
    font-size: 1.6rem;
    font-style: normal;
    font-weight: 400;
    line-height: calc(28 / 16);
    letter-spacing: 0.8px;
  }

  /*  style for scroll  */

  .delivery-cost-table-wrapper {
    overflow: auto;
    display: flex;
    /* justify-content: center; */
    margin-bottom: 1rem;
  }

  /* width */
  .delivery-cost-table-wrapper::-webkit-scrollbar {
    height: 0.6rem;
  }

  /* Track */
  .delivery-cost-table-wrapper::-webkit-scrollbar-track {
    background: #f1f1f1;
  }

  /* Handle */
  ::-webkit-scrollbar-thumb {
    background: #888;
  }

  /* Handle on hover */
  ::-webkit-scrollbar-thumb:hover {
    background: #555;
  }

  /* end style for scroll  */

  .delivery-cost-table {
    display: flex;
    margin-top: 1rem;
    margin-bottom: 2rem;
  }

  .delivery-cost-table-col {
    min-width: 6.8rem;
    text-align: center;
    border-left: 1px solid var(--secondary, #7B7B7B);
  }

  .delivery-cost-table-col.col-first {
    min-width: 14rem;
  }

  .delivery-cost-table-col:last-child {
    border-right: 1px solid var(--secondary, #7B7B7B);
  }

  .delivery-cost-table-col span {
    background-color: #fff;
    display: inline-flex;
    justify-content: center;
    align-items: center;
    width: 100%;
    height: 4rem;
    border-top: 1px solid var(--secondary, #7B7B7B);
    color: #7B7B7B;
    font-family: Oswald;
    font-size: 1.2rem;
    font-style: normal;
    font-weight: 400;
    letter-spacing: 0.6px;
  }
  .delivery-cost-table-col span:last-child {
    border-bottom: 1px solid var(--secondary, #7B7B7B);
  }

  .delivery-cost-table-col.col-first span {
    background: #e8e8e8;
  }

  .delivery-cost-table-col span:first-child {
    color: #fff;
    background-color: #919090;
    border-color: #fff 1px solid;
    border-right: #fff 1px solid;
    font-family: Noto Serif JP;
    font-size: 1.6rem;
    font-weight: 400;
  }

  @media screen and (max-width: 749px) {
    .delivery-cost-txt {
      font-size: 1.4rem;
      line-height: calc(24 / 14);
      letter-spacing: 0.7px;
    }
  }

</style>

{% schema %}
  {
    "name": "Guide delivery cost",
    "tag": "section",
    "class": "section",
    "settings": [
      {
        "type": "color_scheme",
        "id": "section_color_scheme",
        "label": "t:sections.all.colors.label",
        "default": "background-3"
      }
    ],
    "blocks": [
      {
        "type": "Block",
        "name": "Block",
        "settings": [
          {
            "type": "text",
            "id": "delivery-area",
            "default": "北海道",
            "label": "Delivery area"
          },
          {
            "type": "text",
            "id": "delivery-cost-1",
            "default": "1,650円",
            "label": "80(5kg)の配送料"
          },
          {
            "type": "text",
            "id": "delivery-cost-2",
            "default": "1,960円",
            "label": "100(10kg)の配送料"
          },
          {
            "type": "text",
            "id": "delivery-cost-3",
            "default": "2,460円",
            "label": "140(20kg)の配送料"
          }, {
            "type": "text",
            "id": "delivery-cost-4",
            "default": "2,690円",
            "label": "160(30kg)の配送料"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Guide delivery cost",
        "blocks": [
          {
            "type": "Block"
          }, {
            "type": "Block"
          }
        ]
      }
    ]
  }
{% endschema %}